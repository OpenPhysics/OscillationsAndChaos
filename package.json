{
  "name": "OscillationAndChaos",
  "private": true,
  "version": "0.0.0",
  "description": "Interactive Classical Mechanics Simulations using SceneryStack.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "classical mechanics",
    "physics",
    "simulation",
    "SceneryStack",
    "interactive",
    "education",
    "double pendulum"
  ],
  "scripts": {
    "start": "npx vite",
    "build": "npx tsgo && npx vite build",
    "preview": "npx vite preview",
    "lint": "npx eslint .",
    "format": "npx prettier --write .",
    "fix": "npx eslint --fix . && npx prettier --write .",
    "release": "npm version patch && git push && git push --tags",
    "check": "npx tsgo --noEmit",
    "check:tsc": "npx tsc --noEmit",
    "serve": "npx serve -s dist",
    "watch": "npx tsgo --watch",
    "watch:tsc": "npx tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20251105.1",
    "katex": "^0.16.25",
    "scenerystack": "^3.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "eslint": "^9.17.0",
    "globals": "^15.15.0",
    "prettier": "^3.4.2",
    "typescript": "~5.7.2",
    "typescript-eslint": "^8.46.4",
    "vite": "^8.0.16",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "phet-io": {
    "validation": false,
    "compareDesignedAPIChanges": false
  }
}
